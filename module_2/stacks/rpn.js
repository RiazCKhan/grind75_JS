const rpn = (tokens) => {
  let stack = [];
  let signVal = 0
  let result = 0

  for (let i = 0; i < tokens.length; i++) {


  }
}

let testcase = ['8', '2', '/', '3', '+'];
console.log(rpn(testcase))
